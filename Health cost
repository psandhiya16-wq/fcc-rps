# ================================
# Linear Regression Health Costs Calculator
# ================================

import pandas as pd
import numpy as np
from sklearn.model_selection import train_test_split
from sklearn.preprocessing import OneHotEncoder
from sklearn.compose import ColumnTransformer
from sklearn.pipeline import Pipeline
from sklearn.linear_model import LinearRegression
from sklearn.metrics import mean_absolute_error

# ---- 1) Load dataset (already imported in notebook, variable = 'dataset') ----
# Check what columns exist
print("Dataset columns:", dataset.columns.tolist())
print("First rows:\n", dataset.head())

# ---- 2) Split features/labels ----
# Separate features and target
X = dataset.drop('expenses', axis=1)
y = dataset['expenses']

# ---- 3) Handle categorical features ----
categorical_features = ['sex', 'smoker', 'region']
numeric_features = [col for col in X.columns if col not in categorical_features]

# ColumnTransformer applies OneHotEncoder only to categorical cols
preprocessor = ColumnTransformer(
    transformers=[
        ('cat', OneHotEncoder(drop='first'), categorical_features)  # drop first to avoid dummy trap
